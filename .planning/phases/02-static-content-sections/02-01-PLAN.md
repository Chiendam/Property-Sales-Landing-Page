---
phase: 02-static-content-sections
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - css/components.css
  - img/hero-desktop.webp
  - img/hero-desktop.jpg
  - img/hero-mobile.webp
  - img/hero-mobile.jpg
autonomous: true
requirements:
  - NAV-01
  - NAV-02
  - NAV-03
  - NAV-04
  - HERO-01
  - HERO-02
  - HERO-03
  - HERO-04
  - HERO-05

must_haves:
  truths:
    - "Sticky header remains at top of viewport when scrolling (position:sticky)"
    - "Tel: phone link is always visible in header at 375px"
    - "Nav links (6 anchors) appear in desktop header at 768px+"
    - "Hamburger toggle visible on mobile, hidden at 768px+"
    - "Hero section occupies full viewport height on first load at 375x667px"
    - "Dark overlay (rgba(0,0,0,0.45)) visible over hero image"
    - "Hero heading and CTA button fully visible without scrolling at 375x667px"
    - "Mobile sticky CTA bar is fixed at screen bottom on viewports < 768px"
    - "CSS .site-nav--open and .site-nav__link--active classes defined for Phase 3 JS"
  artifacts:
    - path: "img/hero-desktop.webp"
      provides: "Desktop WebP hero image (~400KB at 1200px)"
    - path: "img/hero-mobile.webp"
      provides: "Mobile WebP hero image (~200KB at 800px)"
    - path: "img/hero-desktop.jpg"
      provides: "Desktop JPEG fallback"
    - path: "img/hero-mobile.jpg"
      provides: "Mobile JPEG fallback"
    - path: "index.html"
      provides: "Populated nav + hero HTML"
    - path: "css/components.css"
      provides: "Hero, nav mobile dropdown, mobile CTA bar CSS"
  key_links:
    - from: "index.html .site-nav"
      to: "section IDs"
      via: "anchor href attributes"
      pattern: "href=\"#(services|loan-products|calculator|projects|process|contact)\""
    - from: "index.html .hero"
      to: "img/hero-desktop.webp and img/hero-mobile.webp"
      via: "<picture><source> elements"
      pattern: "srcset=\"img/hero-desktop\\.webp\""
    - from: "index.html .mobile-cta-bar"
      to: "#contact"
      via: "anchor href"
      pattern: "href=\"#contact\""
---

<objective>
Build the sticky navigation (with all anchor links and hamburger CSS scaffold) and the full Hero section with WebP/JPEG background image, dark overlay, and mobile sticky CTA bar.

Purpose: These are the above-the-fold elements — the sticky header and hero CTA drive all conversion. The hero image is the LCP element and must be correct for Phase 5 Lighthouse goals.
Output: Populated nav HTML, hero section with picture element, hero CSS, mobile CTA bar content, nav dropdown CSS scaffold.
</objective>

<execution_context>
@/Users/chiendam/Dev/Projects/real_estate/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chiendam/Dev/Projects/real_estate/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/chiendam/Dev/Projects/real_estate/.planning/ROADMAP.md
@/Users/chiendam/Dev/Projects/real_estate/.planning/phases/02-static-content-sections/02-RESEARCH.md
@/Users/chiendam/Dev/Projects/real_estate/index.html
@/Users/chiendam/Dev/Projects/real_estate/css/main.css
@/Users/chiendam/Dev/Projects/real_estate/css/components.css
@/Users/chiendam/Dev/Projects/real_estate/css/layout.css
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate placeholder hero images and create img/ directory</name>
  <read_first>
    - /Users/chiendam/Dev/Projects/real_estate/index.html (confirm img/ references not yet present)
  </read_first>
  <files>img/hero-desktop.jpg, img/hero-desktop.webp, img/hero-mobile.jpg, img/hero-mobile.webp</files>
  <action>
    Create the img/ directory and download placeholder images from Picsum then convert to WebP using cwebp.

    Run these exact commands:
    ```
    mkdir -p /Users/chiendam/Dev/Projects/real_estate/img

    # Download desktop JPEG (1200x800)
    curl -L "https://picsum.photos/1200/800" -o /Users/chiendam/Dev/Projects/real_estate/img/hero-desktop.jpg

    # Download mobile JPEG (800x600)
    curl -L "https://picsum.photos/800/600" -o /Users/chiendam/Dev/Projects/real_estate/img/hero-mobile.jpg

    # Convert to WebP (desktop: quality 80 targets ~400KB)
    /opt/homebrew/bin/cwebp -q 80 /Users/chiendam/Dev/Projects/real_estate/img/hero-desktop.jpg -o /Users/chiendam/Dev/Projects/real_estate/img/hero-desktop.webp

    # Convert to WebP (mobile: quality 75 targets ~200KB)
    /opt/homebrew/bin/cwebp -q 75 /Users/chiendam/Dev/Projects/real_estate/img/hero-mobile.jpg -o /Users/chiendam/Dev/Projects/real_estate/img/hero-mobile.webp
    ```

    If cwebp conversion fails, proceed with JPEG-only images — the `<picture>` element will fall back to JPEG automatically. WebP files can be regenerated later.

    Note actual file sizes after generation for PERF-01 reference (Phase 5).
  </action>
  <verify>
    <automated>ls -lh /Users/chiendam/Dev/Projects/real_estate/img/</automated>
  </verify>
  <done>img/ directory contains hero-desktop.jpg, hero-desktop.webp, hero-mobile.jpg, hero-mobile.webp. All 4 files exist.</done>
</task>

<task type="auto">
  <name>Task 2: Populate nav links + hero section HTML + mobile CTA bar in index.html</name>
  <read_first>
    - /Users/chiendam/Dev/Projects/real_estate/index.html (read full file before modifying)
  </read_first>
  <files>index.html</files>
  <action>
    Edit index.html to make the following targeted changes. Read the full file first to understand current structure, then apply edits.

    **Change 1 — Populate the empty `<nav class="site-nav">` with anchor links (NAV-01, NAV-03, NAV-04):**

    Replace the empty nav block:
    ```html
    <!-- nav links added in Phase 2 -->
    ```
    With:
    ```html
    <ul class="site-nav__list">
      <li><a href="#services" class="site-nav__link">Dịch Vụ</a></li>
      <li><a href="#loan-products" class="site-nav__link">Gói Vay</a></li>
      <li><a href="#calculator" class="site-nav__link">Máy Tính</a></li>
      <li><a href="#projects" class="site-nav__link">Dự Án</a></li>
      <li><a href="#process" class="site-nav__link">Quy Trình</a></li>
      <li><a href="#contact" class="site-nav__link">Đăng Ký</a></li>
    </ul>
    ```

    **Change 2 — Replace the hero section stub with full content (HERO-01 through HERO-04):**

    Replace the entire `<section id="hero" ...>` block (currently a simple div with placeholder text) with:
    ```html
    <section id="hero" aria-labelledby="hero-heading" class="hero section section--dark">

      <!-- Background image layer: WebP + JPEG fallback, eager + fetchpriority (HERO-01, HERO-02) -->
      <picture class="hero__bg">
        <source
          media="(min-width: 768px)"
          srcset="img/hero-desktop.webp"
          type="image/webp">
        <source
          media="(min-width: 768px)"
          srcset="img/hero-desktop.jpg">
        <source
          srcset="img/hero-mobile.webp"
          type="image/webp">
        <img
          src="img/hero-mobile.jpg"
          alt="Căn hộ hiện đại tại trung tâm thành phố"
          width="800"
          height="600"
          loading="eager"
          fetchpriority="high"
          class="hero__img">
      </picture>

      <!-- Dark overlay via .hero::after CSS pseudo-element (HERO-04) -->

      <!-- Content layer sits above overlay via z-index: 2 (HERO-03) -->
      <div class="hero__content container">
        <h1 id="hero-heading" class="hero__heading">
          Giải Pháp Vay Mua Nhà Ưu Đãi
        </h1>
        <p class="hero__subheading">
          Hỗ trợ vay lên đến 80% giá trị bất động sản — tư vấn miễn phí, xử lý hồ sơ trong 3–5 ngày
        </p>
        <a href="#contact" class="btn btn--primary">Đăng Ký Tư Vấn Miễn Phí</a>
      </div>

    </section>
    ```

    **Change 3 — Populate the mobile CTA bar (HERO-05):**

    Replace the `<div class="mobile-cta-bar" aria-hidden="true">` block with:
    ```html
    <div class="mobile-cta-bar" role="complementary" aria-label="Liên hệ nhanh">
      <a href="tel:+84000000000" class="mobile-cta-bar__phone btn btn--secondary">
        <i class="ph-bold ph-phone" aria-hidden="true"></i>
        Gọi Ngay
      </a>
      <a href="#contact" class="btn btn--primary mobile-cta-bar__cta">
        Đăng Ký Tư Vấn Ngay
      </a>
    </div>
    ```
  </action>
  <verify>
    <automated>grep -c "href=\"#services\"" /Users/chiendam/Dev/Projects/real_estate/index.html && grep -c "hero__bg" /Users/chiendam/Dev/Projects/real_estate/index.html && grep -c "fetchpriority=\"high\"" /Users/chiendam/Dev/Projects/real_estate/index.html && grep -c "mobile-cta-bar__phone" /Users/chiendam/Dev/Projects/real_estate/index.html</automated>
  </verify>
  <done>
    - Nav has 6 anchor links to section IDs
    - Hero section uses &lt;picture&gt; with WebP sources and fetchpriority="high" on the &lt;img&gt;
    - Hero heading id="hero-heading" and CTA link to #contact are present
    - Mobile CTA bar has tel: link and #contact anchor button
  </done>
</task>

<task type="auto">
  <name>Task 3: Add hero CSS and nav mobile dropdown CSS to components.css</name>
  <read_first>
    - /Users/chiendam/Dev/Projects/real_estate/css/components.css (read full file to know where to append)
  </read_first>
  <files>css/components.css</files>
  <action>
    Append the following CSS blocks to the END of css/components.css. Do NOT modify any existing rules.

    ```css
    /* ── Hero section (Phase 2: HERO-01 through HERO-05) ─────────────────────── */

    .hero {
      position: relative;
      min-height: 100svh; /* svh prevents iOS chrome-collapse; 100vh fallback via cascade */
      min-height: 100vh;  /* fallback for browsers without svh support */
      min-height: 100svh;
      display: flex;
      align-items: center;
      overflow: hidden;
    }

    /* Dark overlay (HERO-04): rgba(0,0,0,0.45) for text readability */
    .hero::after {
      content: '';
      position: absolute;
      inset: 0;
      background: rgba(0, 0, 0, 0.45);
      z-index: 1;
    }

    .hero__bg {
      position: absolute;
      inset: 0;
      width: 100%;
      height: 100%;
      z-index: 0;
    }

    .hero__img {
      width: 100%;
      height: 100%;
      object-fit: cover;
    }

    .hero__content {
      position: relative;
      z-index: 2; /* above overlay */
      text-align: center;
      padding-block: var(--space-2xl);
    }

    .hero__heading {
      color: var(--color-gold-light); /* gold-light on dark bg: ~8:1 WCAG AA */
      margin-bottom: var(--space-md);
    }

    .hero__subheading {
      font-size: var(--font-size-xl);
      color: var(--color-white);
      margin-bottom: var(--space-xl);
      max-width: 600px;
      margin-inline: auto;
      margin-bottom: var(--space-xl);
    }

    /* ── Nav links list (populated in Phase 2) ───────────────────────────────── */

    .site-nav__list {
      display: flex;
      align-items: center;
      gap: var(--space-lg);
      list-style: none;
    }

    /* Mobile dropdown state — JS in Phase 3 adds .site-nav--open (NAV-02) */
    @media (max-width: 767px) {
      .site-nav {
        position: absolute;
        top: 64px; /* matches .site-header__inner min-height */
        left: 0;
        right: 0;
        background-color: var(--color-navy-dark);
        padding: var(--space-md) var(--container-padding);
        z-index: 99;
      }

      .site-nav--open {
        display: block;
      }

      .site-nav__list {
        flex-direction: column;
        align-items: flex-start;
        gap: var(--space-sm);
      }

      .site-nav__link {
        display: block;
        padding-block: var(--space-sm);
      }
    }

    /* Active link state — JS in Phase 3 adds .site-nav__link--active (NAV-03) */
    .site-nav__link--active {
      color: var(--color-gold-light);
      border-bottom-color: var(--color-gold);
    }

    /* ── Mobile sticky CTA bar content (HERO-05) ─────────────────────────────── */

    .mobile-cta-bar__phone {
      display: inline-flex;
      align-items: center;
      gap: var(--space-xs);
      flex: 1;
      justify-content: center;
    }

    .mobile-cta-bar__cta {
      flex: 1;
      justify-content: center;
    }
    ```
  </action>
  <verify>
    <automated>grep -c "hero::after" /Users/chiendam/Dev/Projects/real_estate/css/components.css && grep -c "site-nav--open" /Users/chiendam/Dev/Projects/real_estate/css/components.css && grep -c "site-nav__link--active" /Users/chiendam/Dev/Projects/real_estate/css/components.css && grep -c "100svh" /Users/chiendam/Dev/Projects/real_estate/css/components.css</automated>
  </verify>
  <done>
    - .hero::after dark overlay CSS exists
    - .site-nav--open modifier class exists inside @media (max-width: 767px)
    - .site-nav__link--active class exists
    - 100svh used in .hero min-height
    - No existing component rules modified
  </done>
</task>

</tasks>

<verification>
Open index.html in a browser at 375px viewport width. Verify:
1. Header is sticky — scroll down and header stays at top
2. Phone link "Gọi Ngay: [SĐT]" is visible in header
3. Hamburger toggle icon visible; nav links hidden (Phase 3 wires the toggle)
4. Hero section fills the viewport height
5. Dark overlay visible over hero image (image slightly darkened)
6. "Giải Pháp Vay Mua Nhà Ưu Đãi" heading and CTA button both visible without scrolling
7. At bottom of screen: gold "Đăng Ký Tư Vấn Ngay" button and "Gọi Ngay" button in sticky bar
8. At 768px+ viewport: nav links appear in header, hamburger hidden

Grep checks (no browser needed):
- `grep -c "fetchpriority=\"high\"" index.html` → 1
- `grep -c "loading=\"eager\"" index.html` → 1
- `grep -c "site-nav__link" index.html` → 6+
- `grep -c "hero::after" css/components.css` → 1
</verification>

<success_criteria>
- img/ directory contains 4 hero image files (webp + jpg for desktop + mobile)
- index.html nav has 6 anchor links pointing to section IDs
- Hero section uses &lt;picture&gt; element with WebP + JPEG fallback sources
- Hero &lt;img&gt; has loading="eager" fetchpriority="high" width="800" height="600"
- Hero content (h1 + p + .btn--primary) is in a z-index:2 container above the overlay
- .hero::after pseudo-element provides rgba(0,0,0,0.45) overlay (z-index:1)
- .mobile-cta-bar has tel: phone link and #contact anchor
- .site-nav--open and .site-nav__link--active CSS classes defined for Phase 3 JS
</success_criteria>

<output>
After completion, create /Users/chiendam/Dev/Projects/real_estate/.planning/phases/02-static-content-sections/02-01-SUMMARY.md
</output>
