---
phase: 01-html-foundation-design-system
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - index.html
autonomous: true
requirements:
  - FOUND-02
  - FOUND-04
  - FOUND-05
  - FOUND-07

must_haves:
  truths:
    - "Opening index.html in a browser shows no layout errors at 375px, 768px, and 1200px viewports"
    - "Be Vietnam Pro and Montserrat fonts load without render-blocking — preconnect hints precede the Google Fonts stylesheet link"
    - "Phosphor Icons CDN loads and a ph-house icon renders at 24px in the browser"
    - "Vietnamese characters in the heading 'Đăng Ký Tư Vấn Miễn Phí' render correctly using Be Vietnam Pro with diacritics intact"
    - "All 8 page sections exist as semantic HTML with id, aria-labelledby, and matching h2 id attributes"
    - "All 4 script tags use defer — no script blocks HTML parsing"
  artifacts:
    - path: "index.html"
      provides: "Complete semantic HTML shell with CDN embeds, all sections, and correct ARIA landmarks"
      contains: "lang=\"vi\""
    - path: "index.html"
      provides: "Google Fonts non-render-blocking load"
      contains: "preconnect"
    - path: "index.html"
      provides: "Phosphor Icons CDN"
      contains: "phosphor-icons"
  key_links:
    - from: "index.html <head>"
      to: "css/main.css → css/layout.css → css/components.css → css/animations.css"
      via: "<link rel='stylesheet'> in correct cascade order"
      pattern: "css/main\\.css.*css/layout\\.css.*css/components\\.css.*css/animations\\.css"
    - from: "index.html <body>"
      to: "js/nav.js, js/calculator.js, js/animations.js, js/form.js"
      via: "<script src defer> at bottom of body"
      pattern: "defer"
    - from: "<section id='services' aria-labelledby='services-heading'>"
      to: "<h2 id='services-heading'>"
      via: "aria-labelledby matching h2 id"
      pattern: "aria-labelledby=\"services-heading\""
---

<objective>
Write the complete index.html semantic shell — all CDN embeds, all 8 page sections as ARIA landmark regions, and visual sample elements that prove the CSS token layer is working.

Purpose: This is the single HTML file the user will open in a browser to verify Phase 1 is complete. It must render the design system tokens visually (buttons, headings, icon), confirm fonts load, and provide a stable structural anchor for Phase 2 content work.

Output:
- index.html with lang="vi", correct <head> CDN order (preconnect → fonts → Phosphor → CSS), semantic <main> with 8 sections (FOUND-02)
- Google Fonts: Be Vietnam Pro 400+600 + Montserrat 800, subset=vietnamese, display=swap, preconnect hints (FOUND-04)
- Phosphor Icons @2.1.2 CSS bold variant pinned version (FOUND-05)
- Visual sample elements: .btn--primary, .btn--secondary, h2, ph-house icon, Vietnamese text (UI-SPEC Visual Sample Requirements)
- All 4 script tags with defer at bottom of body (FOUND-02)
- Mobile-first HTML structure that renders without horizontal overflow at 375px (FOUND-07)
</objective>

<execution_context>
@/Users/chiendam/Dev/Projects/real_estate/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chiendam/Dev/Projects/real_estate/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-html-foundation-design-system/01-RESEARCH.md
@.planning/phases/01-html-foundation-design-system/01-UI-SPEC.md

<interfaces>
<!-- CSS class contracts established in Plan 01 — use these exact names, never rename -->
<!-- From css/components.css: -->
  .btn, .btn--primary, .btn--secondary
  .site-header, .site-header__inner, .site-header__logo, .site-header__phone, .site-header__nav
  .site-nav, .site-nav__link
  .nav-toggle
  .mobile-cta-bar
  .card
  .site-footer, .site-footer__link, .site-footer__disclaimer

<!-- From css/layout.css: -->
  .container
  .section, .section--light, .section--alt, .section--dark
  .section__heading
  .grid-2, .grid-3

<!-- Section IDs (locked — later phases target these with JS and anchor links): -->
  #hero       aria-labelledby="hero-heading"
  #services   aria-labelledby="services-heading"
  #loan-products  aria-labelledby="loan-products-heading"
  #calculator aria-labelledby="calculator-heading"
  #projects   aria-labelledby="projects-heading"
  #process    aria-labelledby="process-heading"
  #testimonials  aria-labelledby="testimonials-heading"
  #contact    aria-labelledby="contact-heading"

<!-- CDN embed order (order matters — main.css tokens must load before layout/components): -->
  1. <link rel="preconnect" href="https://fonts.googleapis.com">
  2. <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  3. Google Fonts <link href="...">
  4. Phosphor Icons <link rel="stylesheet" href="https://unpkg.com/@phosphor-icons/web@2.1.2/src/bold/style.css">
  5. <link rel="stylesheet" href="css/main.css">
  6. <link rel="stylesheet" href="css/layout.css">
  7. <link rel="stylesheet" href="css/components.css">
  8. <link rel="stylesheet" href="css/animations.css">
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write complete index.html semantic shell</name>
  <files>index.html</files>
  <read_first>
    - index.html (current stub — read before overwriting)
    - css/main.css (verify token names are defined before referencing in class names)
    - .planning/phases/01-html-foundation-design-system/01-UI-SPEC.md (CDN Reference section, Component Inventory, Copywriting Contract, Visual Sample Requirements)
    - .planning/phases/01-html-foundation-design-system/01-RESEARCH.md (Pattern 3 semantic HTML shell, Pattern 4 Google Fonts, Pattern 5 Phosphor Icons, Code Examples: Full head CDN Embed Block)
  </read_first>
  <action>
Replace the stub in index.html with the complete semantic shell. Follow the exact CDN embed order and section structure below.

CRITICAL constraints to enforce:
- Phosphor Icons version MUST be @2.1.2 (not @2.1.1 from UI-SPEC, not @latest)
- fonts.gstatic.com preconnect MUST have `crossorigin` attribute — fonts.googleapis.com preconnect does NOT have it
- Google Fonts URL MUST include `subset=vietnamese` and `display=swap`
- CSS load order MUST be: main.css → layout.css → components.css → animations.css
- ALL script tags MUST use `defer`
- ONE `<main>` wraps all sections
- ONE `<h1>` per page (hero section only)
- Every `<section>` MUST have `id` and `aria-labelledby` matching its `<h2 id>`

Write the following complete index.html:

```html
<!DOCTYPE html>
<html lang="vi">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>[Tên Tổ Chức] — Tư Vấn Vay Mua Nhà</title>

  <!-- Fonts: preconnect first, then stylesheet (FOUND-04) -->
  <!-- fonts.googleapis.com: no crossorigin -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <!-- fonts.gstatic.com: crossorigin required for CORS font files -->
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Be+Vietnam+Pro:wght@400;600&family=Montserrat:wght@800&display=swap&subset=vietnamese" rel="stylesheet">

  <!-- Phosphor Icons v2.1.2 — CSS bold variant, pinned version (FOUND-05) -->
  <!-- DO NOT change to @latest or @2.1.1 — version is pinned per research -->
  <link rel="stylesheet" href="https://unpkg.com/@phosphor-icons/web@2.1.2/src/bold/style.css">

  <!-- CSS: load order matters — tokens (main) before layout before components (FOUND-01) -->
  <link rel="stylesheet" href="css/main.css">
  <link rel="stylesheet" href="css/layout.css">
  <link rel="stylesheet" href="css/components.css">
  <link rel="stylesheet" href="css/animations.css">
</head>
<body>

  <!-- ── Sticky header (Phase 2 populates nav links + phone number) ── -->
  <header class="site-header" role="banner">
    <div class="site-header__inner container">
      <a href="/" class="site-header__logo">[Tên Tổ Chức]</a>

      <!-- Phone always visible in header (NAV-01 — sticky + tel: link) -->
      <a href="tel:+84000000000" class="site-header__phone">
        <i class="ph-bold ph-phone" aria-hidden="true" style="font-size: 18px;"></i>
        Gọi Ngay: [SĐT]
      </a>

      <!-- Nav (Phase 2 populates links, Phase 3 adds hamburger JS) -->
      <nav class="site-nav" aria-label="Điều hướng chính">
        <!-- nav links added in Phase 2 -->
      </nav>

      <!-- Hamburger toggle (Phase 3 adds aria-expanded JS) -->
      <button class="nav-toggle" aria-label="Mở menu" aria-expanded="false" aria-controls="site-nav">
        <i class="ph-bold ph-list" aria-hidden="true"></i>
      </button>
    </div>
  </header>

  <!-- ── Main content ──────────────────────────────────────────────── -->
  <main id="main-content">

    <!-- ── Hero section ──────────────────────────────────────────── -->
    <!-- Background image and overlay added in Phase 2 -->
    <section id="hero" aria-labelledby="hero-heading" class="section section--dark">
      <div class="container">
        <h1 id="hero-heading">[Placeholder: Giải Pháp Vay Mua Nhà Ưu Đãi]</h1>
        <p>[Placeholder: Hỗ trợ vay lên đến 80% giá trị bất động sản]</p>

        <!-- Primary CTA — visual sample #1: gold bg + navy-dark text (UI-SPEC Visual Sample Req #1) -->
        <a href="#contact" class="btn btn--primary">Đăng Ký Tư Vấn Miễn Phí</a>
      </div>
    </section>

    <!-- ── Services section ───────────────────────────────────────── -->
    <section id="services" aria-labelledby="services-heading" class="section section--alt">
      <div class="container">
        <!-- Visual sample #3: h2 with navy color and Be Vietnam Pro (UI-SPEC Visual Sample Req #3) -->
        <h2 id="services-heading" class="section__heading">Dịch Vụ Của Chúng Tôi</h2>
        <!-- Cards added in Phase 2 -->
        <!-- Visual sample #4: Phosphor icon at 24px (UI-SPEC Visual Sample Req #4) -->
        <p>
          <i class="ph-bold ph-house" aria-hidden="true" style="font-size: 24px;"></i>
          <!-- Visual sample #5: Vietnamese diacritics (UI-SPEC Visual Sample Req #5) -->
          Đăng Ký Tư Vấn Miễn Phí
        </p>
        <!-- Visual sample #2: secondary button (UI-SPEC Visual Sample Req #2) -->
        <a href="#contact" class="btn btn--secondary">Tìm Hiểu Thêm</a>
      </div>
    </section>

    <!-- ── Loan products section ──────────────────────────────────── -->
    <section id="loan-products" aria-labelledby="loan-products-heading" class="section section--light">
      <div class="container">
        <h2 id="loan-products-heading" class="section__heading">Gói Vay Ưu Đãi</h2>
        <!-- Product cards added in Phase 2 -->
      </div>
    </section>

    <!-- ── Calculator section ────────────────────────────────────── -->
    <section id="calculator" aria-labelledby="calculator-heading" class="section section--alt">
      <div class="container">
        <h2 id="calculator-heading" class="section__heading">Máy Tính Vay</h2>
        <!-- Calculator inputs added in Phase 2; JS logic in Phase 3 -->
      </div>
    </section>

    <!-- ── Projects section ──────────────────────────────────────── -->
    <section id="projects" aria-labelledby="projects-heading" class="section section--light">
      <div class="container">
        <h2 id="projects-heading" class="section__heading">Dự Án Nổi Bật</h2>
        <!-- Project cards added in Phase 2 -->
      </div>
    </section>

    <!-- ── Process section ───────────────────────────────────────── -->
    <section id="process" aria-labelledby="process-heading" class="section section--alt">
      <div class="container">
        <h2 id="process-heading" class="section__heading">Quy Trình Vay Đơn Giản</h2>
        <!-- Process steps added in Phase 2 -->
      </div>
    </section>

    <!-- ── Testimonials section ──────────────────────────────────── -->
    <section id="testimonials" aria-labelledby="testimonials-heading" class="section section--light">
      <div class="container">
        <h2 id="testimonials-heading" class="section__heading">Khách Hàng Nói Gì</h2>
        <!-- Testimonial cards added in Phase 2 -->
      </div>
    </section>

    <!-- ── Contact / lead form section ──────────────────────────── -->
    <section id="contact" aria-labelledby="contact-heading" class="section section--dark">
      <div class="container">
        <h2 id="contact-heading" class="section__heading">Đăng Ký Tư Vấn</h2>
        <!-- Form fields added in Phase 2; validation + submit in Phase 3 -->
      </div>
    </section>

  </main><!-- /main -->

  <!-- ── Footer (Phase 2 populates content) ──────────────────────── -->
  <footer class="site-footer" role="contentinfo">
    <div class="container">
      <p>[Tên Tổ Chức] — Placeholder address and license. Phase 4 adds legal content.</p>
      <p class="site-footer__disclaimer">
        Lãi suất tham khảo, có thể thay đổi theo điều kiện thực tế. Xem chi tiết tại chi nhánh.
      </p>
      <p>&copy; 2026 [Tên Tổ Chức]</p>
    </div>
  </footer>

  <!-- ── Mobile sticky CTA bar (Phase 2 adds content) ───────────── -->
  <div class="mobile-cta-bar" aria-hidden="true">
    <!-- Tel link + scroll-to-form button added in Phase 2 -->
  </div>

  <!-- ── Scripts: ALL use defer (FOUND-02) ────────────────────────── -->
  <script src="js/nav.js" defer></script>
  <script src="js/calculator.js" defer></script>
  <script src="js/animations.js" defer></script>
  <script src="js/form.js" defer></script>

</body>
</html>
```
  </action>
  <verify>
    <automated>grep -c 'lang="vi"' /Users/chiendam/Dev/Projects/real_estate/index.html && grep -c 'defer' /Users/chiendam/Dev/Projects/real_estate/index.html && grep -c 'preconnect' /Users/chiendam/Dev/Projects/real_estate/index.html && grep -c 'aria-labelledby' /Users/chiendam/Dev/Projects/real_estate/index.html && grep -c 'phosphor-icons/web@2.1.2' /Users/chiendam/Dev/Projects/real_estate/index.html && grep -c 'subset=vietnamese' /Users/chiendam/Dev/Projects/real_estate/index.html && echo "INDEX.HTML COMPLETE"</automated>
  </verify>
  <acceptance_criteria>
    - `grep 'lang="vi"' index.html` returns exactly 1 result on the `<html>` opening tag
    - `grep -c 'defer' index.html` returns 4 (one per script tag)
    - `grep 'preconnect.*fonts.googleapis.com' index.html` returns 1 line with NO crossorigin attribute
    - `grep 'preconnect.*fonts.gstatic.com' index.html` returns 1 line WITH `crossorigin` attribute
    - `grep 'subset=vietnamese' index.html` returns 1 line in the Google Fonts href
    - `grep 'display=swap' index.html` returns 1 line in the Google Fonts href
    - `grep 'phosphor-icons/web@2.1.2' index.html` returns 1 line (pinned version, not @latest or @2.1.1)
    - `grep -c 'aria-labelledby' index.html` returns 8 (one per section)
    - `grep 'id="hero"' index.html` and `grep 'id="hero-heading"' index.html` both return 1 result each
    - `grep 'id="contact"' index.html` and `grep 'id="contact-heading"' index.html` both return 1 result each
    - `grep 'css/main.css' index.html` appears BEFORE `css/layout.css` in file order (token cascade)
    - `grep '<main' index.html` returns exactly 1 result (one main element)
    - `grep '<h1' index.html` returns exactly 1 result (hero only)
    - `grep 'ph-bold ph-house' index.html` returns 1 line (Phosphor icon visual sample)
    - `grep 'Đăng Ký Tư Vấn Miễn Phí' index.html` returns at least 1 line (primary CTA copy + Vietnamese characters)
    - `grep 'btn--primary' index.html` returns at least 1 line
    - `grep 'btn--secondary' index.html` returns at least 1 line
  </acceptance_criteria>
  <done>index.html contains lang="vi", 2 preconnect hints (gstatic with crossorigin), Google Fonts URL with subset=vietnamese and display=swap, Phosphor Icons @2.1.2, 4 CSS links in correct cascade order, <main> wrapping 8 sections each with id+aria-labelledby+matching h2 id, visual sample elements (.btn--primary, .btn--secondary, ph-house icon, Vietnamese CTA text), and 4 script tags with defer. No raw styles other than inline font-size on icon demos.</done>
</task>

</tasks>

<verification>
After task completes, run these checks from the project root:

```bash
# 1. lang="vi" present
grep 'lang="vi"' index.html

# 2. 4 defer script tags
grep -c 'defer' index.html
# Expected: 4

# 3. Correct preconnect structure
grep 'preconnect' index.html
# Expected: 2 lines — first without crossorigin, second WITH crossorigin

# 4. Vietnamese subset in font URL
grep 'subset=vietnamese' index.html
# Expected: 1 result

# 5. Phosphor pinned to 2.1.2
grep 'phosphor-icons/web@2.1.2' index.html
# Expected: 1 result

# 6. 8 sections with aria-labelledby
grep -c 'aria-labelledby' index.html
# Expected: 8

# 7. CSS load order correct
grep -n 'rel="stylesheet"' index.html
# Expected: main.css line number < layout.css < components.css < animations.css

# 8. One h1, one main
grep -c '<h1' index.html
# Expected: 1
grep -c '<main' index.html
# Expected: 1

# 9. Visual samples present
grep 'btn--primary\|btn--secondary\|ph-house' index.html
# Expected: 3 matches
```
</verification>

<success_criteria>
- index.html opens in a browser without errors in the console (beyond expected "no content" network requests for placeholder images)
- At 375px viewport: page scrolls vertically, no horizontal overflow, all section headings visible with correct Vietnamese characters
- At 768px viewport: layout adjusts (container padding wider), no visual breakage
- At 1200px viewport: container max-width constrains content to 1200px centered
- .btn--primary renders gold background with dark navy text
- .btn--secondary renders navy background with white text
- ph-house icon renders as a house glyph (not a box or question mark)
- "Đăng Ký Tư Vấn Miễn Phí" text shows correct diacritics in Be Vietnam Pro font
- h2 section headings render in navy color at approximately 28–40px fluid size
</success_criteria>

<output>
After completion, create `.planning/phases/01-html-foundation-design-system/01-02-SUMMARY.md`
</output>
