---
phase: 02-static-content-sections
plan: 05
type: execute
wave: 2
depends_on:
  - 02-01
  - 02-02
  - 02-03
  - 02-04
files_modified: []
autonomous: false
requirements: []

must_haves:
  truths:
    - "All 8 sections render at 375px, 768px, 1200px without horizontal overflow"
    - "Hero CTA is visible without scrolling at 375x667px viewport"
    - "Sticky header remains at top on scroll"
    - "Mobile sticky CTA bar is fixed at bottom on 375px viewport"
    - "No raw hex values or px values appear outside CSS custom properties in component rules"
    - "No JavaScript errors in browser console (JS files are empty stubs)"
  artifacts:
    - path: "index.html"
      provides: "Complete 8-section page verified by human"
    - path: "css/components.css"
      provides: "All Phase 2 component styles verified in browser"
---

<objective>
Human visual verification of all 8 sections across 3 breakpoints. Confirms the complete static page is visually correct before Phase 3 adds JavaScript.

Purpose: This checkpoint catches visual issues (overflow, color contrast failures, missing sections, broken images) before JS is layered on. Issues found here are easier to fix than after Phase 3 adds interactivity.
Output: Confirmed sign-off or list of issues to fix.
</objective>

<execution_context>
@/Users/chiendam/Dev/Projects/real_estate/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chiendam/Dev/Projects/real_estate/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/chiendam/Dev/Projects/real_estate/.planning/ROADMAP.md
@/Users/chiendam/Dev/Projects/real_estate/index.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Automated structure checks before human review</name>
  <read_first>
    - /Users/chiendam/Dev/Projects/real_estate/index.html
    - /Users/chiendam/Dev/Projects/real_estate/css/components.css
  </read_first>
  <files></files>
  <action>
    Run the following grep checks to confirm all structural requirements are met before asking for human review. All checks must pass (exit 0 or return expected count).

    Run each command and report the result:

    ```bash
    # 1. All 8 sections present with correct IDs
    grep -c 'id="\(hero\|services\|loan-products\|calculator\|projects\|process\|testimonials\|contact\)"' /Users/chiendam/Dev/Projects/real_estate/index.html

    # 2. Hero uses eager + fetchpriority (LCP element — NOT lazy)
    grep 'loading="eager"' /Users/chiendam/Dev/Projects/real_estate/index.html
    grep 'fetchpriority="high"' /Users/chiendam/Dev/Projects/real_estate/index.html

    # 3. All below-fold images use loading="lazy" with explicit dimensions
    grep -c 'loading="lazy"' /Users/chiendam/Dev/Projects/real_estate/index.html
    # Expected: 3 (one per project card)

    # 4. Mobile CTA bar has tel: link and #contact anchor
    grep 'href="tel:' /Users/chiendam/Dev/Projects/real_estate/index.html
    grep 'mobile-cta-bar' /Users/chiendam/Dev/Projects/real_estate/index.html

    # 5. Nav has 6 anchor links
    grep -c 'site-nav__link' /Users/chiendam/Dev/Projects/real_estate/index.html

    # 6. Lead form has 4 field IDs
    grep 'id="field-name"' /Users/chiendam/Dev/Projects/real_estate/index.html
    grep 'id="field-phone"' /Users/chiendam/Dev/Projects/real_estate/index.html
    grep 'id="field-project"' /Users/chiendam/Dev/Projects/real_estate/index.html
    grep 'id="field-loan-amount"' /Users/chiendam/Dev/Projects/real_estate/index.html

    # 7. Submit button text is exact
    grep 'Đăng Ký Tư Vấn Miễn Phí' /Users/chiendam/Dev/Projects/real_estate/index.html
    # Expected: 3 matches (hero CTA + mobile CTA + form submit)

    # 8. Rate disclaimer on loan cards (3 required)
    grep -c 'Thông tư 39/2016' /Users/chiendam/Dev/Projects/real_estate/index.html
    # Expected: 4 (3 loan cards + 1 footer)

    # 9. font-size: 16px on form inputs in CSS
    grep -c 'font-size: 16px' /Users/chiendam/Dev/Projects/real_estate/css/components.css
    # Expected: 2+ (.calc-form__input and .lead-form__input)

    # 10. No raw hex values in component rules (only in :root of main.css)
    grep '#[0-9A-Fa-f]\{3,6\}' /Users/chiendam/Dev/Projects/real_estate/css/components.css
    # Expected: ONLY rgba() values for overlay and shadows — no plain hex like #0D2B5E
    ```

    If any check fails (section missing, counts wrong, form field ID missing), fix the issue in index.html or css/components.css before proceeding to the human checkpoint.
  </action>
  <verify>
    <automated>grep -c 'id="hero"' /Users/chiendam/Dev/Projects/real_estate/index.html && grep -c 'id="contact"' /Users/chiendam/Dev/Projects/real_estate/index.html && grep -c 'fetchpriority="high"' /Users/chiendam/Dev/Projects/real_estate/index.html</automated>
  </verify>
  <done>All automated checks pass. 8 section IDs present, hero has eager+fetchpriority, 3 lazy images, form has 4 fields, no bare hex values in components.css outside rgba().</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human visual verification — all 8 sections</name>
  <what-built>
    Complete static HTML + CSS landing page with all 8 sections:
    1. Sticky navigation header with phone link, desktop nav links, hamburger toggle
    2. Hero section — full-viewport, dark overlay, heading, CTA button
    3. Services — 3 icon cards + stats bar (500+, 200+, 10+)
    4. Loan Products — 3 cards with gold left border, rates, disclaimers
    5. Calculator — 4 input fields, result display area
    6. Projects — 3 image cards with lazy-loaded WebP photos
    7. Process — 4-step timeline with navy number bubbles
    8. Testimonials — 3 review cards with star ratings
    9. Contact form — 4 fields, consent checkbox, submit button
    10. Footer — brand name, quick links, disclaimer, copyright
    11. Mobile sticky CTA bar — phone + registration button at screen bottom
  </what-built>
  <how-to-verify>
    Open /Users/chiendam/Dev/Projects/real_estate/index.html in a browser. Perform each check:

    **375px (iPhone SE) viewport:**
    1. Header sticky — scroll down, header stays at top of screen
    2. Phone "Gọi Ngay" link visible in header
    3. Hero heading and gold CTA button both visible without scrolling
    4. Dark overlay on hero image (image slightly darkened, text readable)
    5. Mobile CTA bar fixed at screen bottom (phone + registration buttons)
    6. Services section: 3 cards stacked vertically, off-white background
    7. Loan products: 3 cards stacked, each has visible gold left border
    8. Calculator: 4 labeled inputs visible
    9. Projects: 3 cards with images stacked, each has name + location + price
    10. Process: 4 steps stacked vertically with numbered circles
    11. Testimonials: 3 cards stacked with star ratings, initials avatars
    12. Contact form: 4 fields stacked, full-width gold submit button
    13. Footer visible with content
    14. NO horizontal scrollbar at 375px

    **768px (tablet) viewport:**
    15. Nav links visible in header (desktop nav shown, hamburger hidden)
    16. Services: 3-column grid
    17. Loan products: 3-column grid
    18. Projects: 3-column grid
    19. Process: 4-column grid
    20. Testimonials: 3-column grid
    21. Mobile CTA bar: hidden (only appears at &lt; 768px)

    **1200px (desktop) viewport:**
    22. All grids 3-column (or 4-column for process)
    23. Content centered within max-width container
    24. Hero image fills viewport at desktop width

    **JavaScript check:**
    25. Open browser DevTools Console — no errors (JS stubs are empty, not broken)

    **Color spot-check:**
    26. Gold left border visible on loan cards
    27. Service card icons are dark gold (not bright yellow)
    28. Star ratings are dark gold (not bright yellow)
    29. Heading text is navy (not black)
  </how-to-verify>
  <resume-signal>Type "approved" if all checks pass, or describe specific issues found (e.g. "loan card border not visible", "footer missing disclaimer").</resume-signal>
</task>

</tasks>

<verification>
All automated checks from Task 1 pass AND user types "approved" at the checkpoint.
</verification>

<success_criteria>
- All 20 Phase 2 requirements (NAV-01 through TEST-02) are visually confirmed in browser
- No horizontal overflow at 375px
- Hero CTA visible above fold at 375x667px
- Gold left border visible on all 3 loan cards
- Mobile sticky CTA bar fixed at bottom on mobile, hidden on desktop
- No JS console errors
- Phase 2 is complete — ready for Phase 3 JavaScript
</success_criteria>

<output>
After completion, create /Users/chiendam/Dev/Projects/real_estate/.planning/phases/02-static-content-sections/02-05-SUMMARY.md
</output>
