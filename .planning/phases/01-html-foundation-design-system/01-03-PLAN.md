---
phase: 01-html-foundation-design-system
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
autonomous: false
files_modified: []
requirements:
  - FOUND-06
  - FOUND-07

must_haves:
  truths:
    - "Opening index.html in a browser at 375px shows no horizontal overflow and all sections are visible"
    - "Netlify deploy succeeds and HTTP response headers include X-Frame-Options and X-Content-Type-Options"
    - "Be Vietnam Pro font renders Vietnamese diacritics correctly in the browser"
    - "Phosphor ph-house icon renders as a house glyph (not an empty box) in the browser"
    - "CSS tokens are visually applied — gold button, navy section headings, off-white alternating sections are visible"
  artifacts:
    - path: "index.html"
      provides: "Verified working page at three breakpoints"
      contains: "lang=\"vi\""
    - path: "netlify.toml"
      provides: "Security headers confirmed in HTTP response"
      contains: "X-Frame-Options"
  key_links:
    - from: "Netlify deploy URL"
      to: "netlify.toml security headers"
      via: "curl -I response headers"
      pattern: "X-Frame-Options: DENY"
---

<objective>
Verify Phase 1 is complete through two gates: (1) visual browser check at three breakpoints, and (2) Netlify deploy confirming security headers are live in the HTTP response.

Purpose: Phase 1 success criteria require both visual token verification and confirmed Netlify headers. Neither can be verified by grep alone — the browser check confirms fonts, icons, and responsive layout work; the curl check confirms the netlify.toml syntax is valid and headers are applied by the CDN.

Output: Human confirms visual correctness at 375px/768px/1200px and that security headers appear in the deployed response.
</objective>

<execution_context>
@/Users/chiendam/Dev/Projects/real_estate/.claude/get-shit-done/workflows/execute-plan.md
@/Users/chiendam/Dev/Projects/real_estate/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-html-foundation-design-system/01-RESEARCH.md
@.planning/phases/01-html-foundation-design-system/01-01-SUMMARY.md
@.planning/phases/01-html-foundation-design-system/01-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Visual browser verification at three breakpoints</name>
  <files>index.html</files>
  <read_first>
    - .planning/phases/01-html-foundation-design-system/01-UI-SPEC.md (Visual Sample Requirements section — exact criteria for what must be visible)
  </read_first>
  <action>
Human verification task — no code changes. Open index.html in a browser and verify the 12 checks listed in how-to-verify. If checks fail, fix the specific CSS or HTML issue before re-running the checkpoint.
  </action>
  <what-built>
    Complete Phase 1 file scaffold:
    - 11 source files created (index.html, 4 CSS files, 4 JS files, netlify.toml, img/.gitkeep)
    - css/main.css with full :root token layer (13 colors, 7 spacing, 4 typography, shadows, radii, transitions)
    - css/layout.css with responsive container and section utilities
    - css/components.css with .btn system and structural component skeletons
    - css/animations.css with [data-animate] scroll-reveal and prefers-reduced-motion guard
    - index.html with semantic HTML shell, CDN embeds, 8 sections, visual sample elements
  </what-built>
  <how-to-verify>
Open index.html in your browser (double-click the file or run `open index.html` in terminal).

Check each of the following — report which pass and which fail:

**At 375px viewport width (iPhone SE)** — use browser DevTools responsive mode:
1. Page scrolls vertically. No horizontal scrollbar appears. No content is cut off on the right edge.
2. The gold button "Đăng Ký Tư Vấn Miễn Phí" is visible and renders with a gold/amber background color.
3. A navy "Tìm Hiểu Thêm" button is visible.
4. The section heading "Dịch Vụ Của Chúng Tôi" renders in dark navy color with Vietnamese characters intact (ụ ủ not showing as boxes).
5. A house icon (ph-house) renders as a visible house glyph — NOT an empty box or question mark.
6. The sticky header is visible at the top of the page with navy background.
7. The services section has an off-white/cream background (different from the white hero section).

**At 768px viewport width (iPad portrait)**:
8. No horizontal overflow. Content is contained within the viewport.
9. All sections from step 1–7 still look correct.

**At 1200px viewport width (desktop)**:
10. Content is centered with equal left/right margins — does not stretch full-width.
11. All sections from step 1–7 still look correct.

**Font check** (any viewport):
12. The text "Đăng Ký Tư Vấn Miễn Phí" shows correct Vietnamese diacritics using a sans-serif font (not a serif fallback font). The characters ă, ế, ấ, ữ should have correct accent marks.

Type "approved" if all 12 checks pass.
If any check fails, describe which number failed and what you see instead.
  </how-to-verify>
  <verify>
    <automated>grep -c 'btn--primary' /Users/chiendam/Dev/Projects/real_estate/index.html && grep -c 'ph-bold ph-house' /Users/chiendam/Dev/Projects/real_estate/index.html && echo "PRE-CHECK PASSED — proceed to browser"</automated>
  </verify>
  <done>Human confirms all 12 visual checks pass: gold button renders, navy headings render, ph-house icon renders, Vietnamese diacritics render correctly, no horizontal overflow at 375px/768px/1200px.</done>
  <acceptance_criteria>
    - User types "approved" confirming all 12 visual checks pass
    - OR user describes specific failures for Claude to fix before re-checking
  </acceptance_criteria>
  <resume-signal>Type "approved" to proceed to Netlify deploy verification, or describe the specific issues observed.</resume-signal>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Netlify deploy + security header verification</name>
  <files>netlify.toml</files>
  <read_first>
    - netlify.toml (verify the four headers are present before asking user to deploy)
    - .planning/phases/01-html-foundation-design-system/01-RESEARCH.md (Pitfall 1: TOML Indentation, Environment Availability section)
  </read_first>
  <action>
Human verification task — no code changes. Verify netlify.toml has correct TOML syntax, then guide user through Netlify deploy and curl header check. If headers are missing from the curl response, check TOML indentation (Pitfall 1 from RESEARCH.md) and redeploy.
  </action>
  <what-built>
    netlify.toml with security headers block:
    - X-Frame-Options: DENY
    - X-Content-Type-Options: nosniff
    - X-XSS-Protection: 1; mode=block
    - Referrer-Policy: strict-origin-when-cross-origin
  </what-built>
  <how-to-verify>
Deploy the project to Netlify (drag-and-drop the project folder to the Netlify dashboard, or run `npx netlify-cli deploy --prod` if you have a Netlify account connected).

Once deployed, run this command with your deploy URL (replace {deploy-url} with the actual URL):

```bash
curl -I https://{deploy-url}
```

In the response headers, check for these exact headers:
- `x-frame-options: DENY` (or `X-Frame-Options: DENY`)
- `x-content-type-options: nosniff` (or `X-Content-Type-Options: nosniff`)
- `x-xss-protection: 1; mode=block` (or `X-XSS-Protection: 1; mode=block`)

All three must appear. The Referrer-Policy header is a bonus but not strictly required for FOUND-06.

If Netlify CLI is not installed, you can deploy via:
1. Go to app.netlify.com
2. Drag the project folder onto the "Deploy manually" drop zone
3. Copy the preview URL
4. Run `curl -I {preview-url}` in terminal

Type "approved" with the deploy URL if all three headers appear in the curl output.
  </how-to-verify>
  <verify>
    <automated>grep -c 'X-Frame-Options' /Users/chiendam/Dev/Projects/real_estate/netlify.toml && grep -c 'X-Content-Type-Options' /Users/chiendam/Dev/Projects/real_estate/netlify.toml && grep -c 'headers.values' /Users/chiendam/Dev/Projects/real_estate/netlify.toml && echo "TOML PRE-CHECK PASSED — proceed to deploy"</automated>
  </verify>
  <done>User confirms curl -I {deploy-url} output contains x-frame-options: DENY, x-content-type-options: nosniff, and x-xss-protection: 1; mode=block. Netlify deploy URL recorded.</done>
  <acceptance_criteria>
    - User confirms `curl -I {deploy-url}` output contains `x-frame-options: DENY`
    - User confirms output contains `x-content-type-options: nosniff`
    - User confirms output contains `x-xss-protection: 1; mode=block`
    - User provides the Netlify deploy URL for future reference
  </acceptance_criteria>
  <resume-signal>Type "approved {deploy-url}" (e.g., "approved https://my-site.netlify.app") once headers are confirmed. Or describe what headers are missing if curl shows unexpected output.</resume-signal>
</task>

</tasks>

<verification>
Phase 1 is complete when both checkpoints are approved:
1. Visual verification: browser shows correct tokens (gold button, navy headings, icons, Vietnamese fonts) at 375px/768px/1200px without horizontal overflow
2. Netlify headers: curl confirms X-Frame-Options, X-Content-Type-Options, X-XSS-Protection in HTTP response
</verification>

<success_criteria>
All 5 Phase 1 success criteria from ROADMAP.md are met:
1. Opening index.html shows no layout errors at 375px, 768px, 1200px viewports — verified by human checkpoint Task 1
2. All CSS custom properties (navy, gold, white, typography, spacing, shadows) are defined in :root and visually applied to sample elements — verified by visual inspection of buttons and headings
3. Be Vietnam Pro and Montserrat load without render-blocking; Vietnamese characters render correctly — verified by visual font check in browser
4. Phosphor Icons CDN loaded and ph-house icon renders at correct size — verified by visual icon check
5. Netlify deploy succeeds with X-Frame-Options, X-Content-Type-Options security headers in HTTP response — verified by curl check
</success_criteria>

<output>
After completion, create `.planning/phases/01-html-foundation-design-system/01-03-SUMMARY.md`
</output>
